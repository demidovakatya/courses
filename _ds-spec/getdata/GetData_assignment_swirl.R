Dplyr library
tbl_df(): data frame tbl
select(): to select a subset of columns
filter(): to select a subset of rows
arrange(): to order the rows of a dataset to the values of a particular variable
mutate(): to create a new variable based on the value of one or more variables
| already in a dataset
summarize(): collapses the dataset to a single row
group_by(): to break up your dataset into 
| groups of rows based on the values of one or more variables
n(): number of observations
n_distinct(): number of distinct observations
gather(): Gather columns into key-value pairs.